#include "main.h"

/**
 * _abs - compute absolute value of an integer
 * @t: the iinteger to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
